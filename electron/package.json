{
  "name": "avas-electron",
  "version": "0.1.0",
  "private": true,
  "description": "AVAS Electron desktop shell",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "electron .",
    "dev": "npm run build && npm run start"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "electron": "^30.0.0",
    "typescript": "^5.4.0"
  }
}
